name: video_player_platform_interface_mux
description: A common platform interface for the video_player plugin.
repository: https://github.com/hungryemon/video_player_platform_interface_mux
issue_tracker: https://github.com/hungryemon/video_player_platform_interface_mux/issues
# NOTE: We strongly prefer non-breaking changes, even at the expense of a
# less-clean API. See https://flutter.dev/go/platform-interface-breaking-changes
version: 1.0.0

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
